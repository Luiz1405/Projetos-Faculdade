/* Exercicio002
  Luiz Augustou de Souza Kubaszewski
  Criei um programa que que le um numero inteiro N correspondendo ao tamanho de um vetor de inteiros e, na sequencia, os N valores inteiros desse vetor e tambem implementei o algoritmo bublle sorte.
  21/09/2023
  */
/*#include <iostream>

void bubbleSort(int* dados, int tam) {
    int trocou;
    do {
        trocou = 0;
        for (int i = 0; i < tam - 1; ++i) {
            if (dados[i] < dados[i + 1]) {
                int aux = dados[i];
                dados[i] = dados[i + 1];
                dados[i + 1] = aux;
                trocou = 1;
            }
        }
        for (int i = 0; i < tam; ++i) {
            std::cout << dados[i] << " ";
        }
        std::cout << std::endl;
    } while (trocou);
}

int main() {
    int N;
    std::cin >> N;

    int vetor[N];

    for (int i = 0; i < N; ++i) {
        std::cin >> vetor[i];
    }

    bubbleSort(vetor, N);

    return 0;
}

*/















